{
  "name": "web3-helper",
  "version": "1.1.0",
  "description": "Helper for web3.js",
  "main": "dist/index.js",
  "scripts": {
    "install": "npm run build",
    "build": "babel src --minified -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
